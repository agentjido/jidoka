# Optional dotenv template for the Phoenix example app.
#
# The Jidoka package does not load .env files itself. For package live tests,
# export provider keys in the shell or configure ReqLLM in your host app.

# ReqLLM provider keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
